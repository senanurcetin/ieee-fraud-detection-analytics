name: ieee_fraud_detection
version: "1.0.0"
config-version: 2

profile: ieee_fraud_detection

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  ieee_fraud_detection:
    staging:
      +schema: staging
      +materialized: view
    intermediate:
      +schema: intermediate
      +materialized: table
    marts:
      +schema: mart
      +materialized: table
